rats_assets_dir = os.getenv("RATS_ASSETS_DIR")

Camera("/Scene/rendering/camera") {
    ["node_xform"] = blur(
        rotate(180, 0, 0, 1) * translate(-3.0, 8.5, 20),
        rotate(0, 0, 0, 1) * translate(0.0, 8.5, 10)),
}

sphere = SphereGeometry("/Scene/geometry/sphereMm") {
    ["node_xform"] = scale(0.5, 0.5, 0.5) * translate(0, 10, 0),
}

GeometrySet("geometrySet") {
    sphere,
}

Layer("layer") {
    { sphere, "", torusMtl, lightSet },
}
